{
  "name": "@neatcode/backend",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "@neatcode/db": "workspace:*"
  },
  "nx": {
    "implicitDependencies": [
      "@neatcode/db"
    ],
    "targets": {
      "build": {
        "executor": "nx:run-commands",
        "options": {
          "command": "webpack-cli build",
          "args": [
            "--node-env=production"
          ]
        },
        "configurations": {
          "development": {
            "args": [
              "--node-env=development"
            ]
          }
        }
      },
      "serve": {
        "continuous": true,
        "executor": "@nx/js:node",
        "defaultConfiguration": "development",
        "dependsOn": [
          "build"
        ],
        "options": {
          "buildTarget": "@neatcode/backend:build",
          "runBuildTargetDependencies": false
        },
        "configurations": {
          "development": {
            "buildTarget": "@neatcode/backend:build:development"
          },
          "production": {
            "buildTarget": "@neatcode/backend:build:production"
          }
        }
      }
    }
  }
}
